# PROMPT — このテンプレートから世界を作る

> 使い方：Claude Code に「https://github.com/n416/explore3d-template から落として PROMPT.md を読んで始めて」と伝える。
> 以下は Claude への指示。

**準備はあなた（Claude）がやる。** まだクローンしていなければ、ユーザーの作業フォルダにクローンし（フォルダ名は世界が決まってから
変えてよい）、`npm install` と `npx playwright install chromium` を実行する。ユーザーにコマンドを打たせない。

---

## あなたへの依頼

このリポジトリは **歩ける／乗れる「アニメ背景風」3D 世界のテンプレート** です。
three.js + Vite + TypeScript、セル調シェーディング、深度の二階差分による墨線、時刻インジケーター、
季節、乗り物、WebXR（VR）、検証 API がすでに動いています（中身は汎用の田舎道のデモ）。

**ゼロから作り直さないでください。** ユーザーの場所・乗り物・季節・テイストに合わせて
このテンプレートを**改変**し、その人の世界にしてください。
最初に `CLAUDE.md` と `README.md` を読むこと（構成・規約・既知の罠・検証方法が書いてある）。

---

## Phase 0：ヒアリング（ここだけは必ずユーザーに聞く）

実装の前に、以下を **1 問ずつ順番に** 質問してください（選択肢があるものは選択式で）。

1. **どうやって動き回りますか？**（自由回答で受ける。選択肢は例にすぎない）
   - 乗り物 → どれに？（ママチャリ / 原付 / 軽トラ / 飛行機 / キックボード / 巨大ロボット / その他なんでも）
   - アクション → 糸でビルの間を飛び回る、壁を走る、屋根を跳び移る…なども可
   - 徒歩で散策
   - 両方 → 乗り物＋降りて歩ける（F で乗り降り）

   一覧に無い動き方は、`src/player/vehicle.ts` に種類を足し、必要なら `player.ts` に動き方（飛行・ジャンプ・スイングなど）を足して作る。
   断らない。実在のキャラクターそのもの（姿・名前・ロゴ）は出さず、動き方だけを取り入れる。
2. **イメージする場所の画像を貼ってください（10 枚くらい）。**
   Google マップのストリートビューを **進む順番に** スクショしたもの。
   目安：道の正面 5〜6 枚、交差点や曲がり角 1〜2 枚、象徴的な建物や景色 1〜2 枚、**上空写真 1 枚**（道の形が分かるもの）。
   1 枚ごとにひと言メモがあると精度が上がる（「右に酒屋、左は田んぼ」など）。
3. **出したいテイストの画像があれば貼ってください（任意、3〜10 枚）。**
   無ければ選択式：ジブリ風の田舎 / 新海誠風の光と空 / セル調・紫の影・濃い墨線 / その他（言葉で）
4. **季節は？** 春（桜） / 初夏（田植え後） / 真夏（入道雲・蝉） / 秋（紅葉・稲刈り） / 冬（雪）
5. **場所はどのあたり？**（都道府県か市町村くらいで可。緯度から太陽の高さを出すため）
6. **VR ヘッドセットは？** Quest 単体 / PC VR / まだ無い（後で対応できればよい）
7. **人は登場させますか？** 出さない / 背景に少しだけ / 主人公だけ
8. **作り込みレベルは？**
   - ラフ：配置・道の形・雰囲気・時刻変化の確認まで（イテレーション 3 回）
   - 標準：場所らしさトップ 10 の物を DETAIL.md で作り込む（5 回＋トップ 10 のディテールアップ）
   - じっくり：画面に映る全部品を作り込み、近寄っても破綻しない密度に（8 回以上＋全カテゴリのディテールアップ。時間とトークンを多く使うことを伝える）

**画像の保存**：チャットに貼られた画像は見えるが、Critic がスクリプトで比較するにはファイルが要る。
貼られた画像はあなたが `refs/place/`（場所）と `refs/style/`（テイスト）に 01.jpg, 02.jpg… の名前で保存する
（貼られた画像はセッションの一時フォルダにファイルとして残っていることが多い。webp なら ffmpeg で jpg に変換）。
ファイルが見つからないときだけ、ユーザーに置いてもらうようお願いする（`.gitignore` 済みなので公開されない）。

### ルールはプロジェクトごとに決める

禁止事項やテイストの約束は **場所と出したいテイストで変わる**（田舎の道にネオンは要らないが、
夜の繁華街ならネオンこそ主役、など）。固定のルールは持ち込まず、ヒアリングの答えと画像から
**あなたが案を作り、ユーザーに確認してもらう**：

- **この世界の約束**（例：「影は紫寄り、線は濃く、看板は手描き風」「写実的な質感は使わない」）
- **禁止事項**（例：「ネオン禁止」「高層ビル禁止」「人物を出さない」「実在の店名・ブランドを出さない」）
- **場所らしさトップ 10**（削ってはいけない要素）

テンプレート側で常に守る最低限は次の 2 つだけ：
- 実在の店名・ブランド・ロゴは使わない（看板の文字は架空にする）。
- Google マップの画像はリポジトリに入れない。

答え・ルール・トップ 10 を `docs/brief.md` にまとめ、ユーザーの OK をもらってから Phase 1 へ。
**それ以降は、止まらないと進めない場合を除いて質問せずに作り切る。**

---

## Phase 1：調査と設定

1. すべての参考画像を見て `docs/survey.md` を書く：1 枚ごとの「写っている要素」「コース上のどこか」「スケールの手がかり」。
   `refs/place/` は **何があるか**（配置・種類・大きさ・道の形）、`refs/style/` は **どう描くか**（色・光・線・空気感）。
   **写真の写実的な質感や色は真似しない**。
2. `src/config.ts` を答えで埋める（season / latitude / mobility / vehicle / people / title）。
3. `src/world/course.ts` を上空写真から起こす：道の中心線（メートル、+x 東・−z 北）、道幅、
   `viewpoints` を 10 個（`refs/place/01〜10` と同じ位置・向き）。
4. テイストに合わせて `src/render/palette.ts`（色）と `src/world/timeofday.ts` の `KEYS`
   （時刻ごとの空・光・影の色）を調整する。線の強さは `src/render/post.ts`。

## Phase 2：世界を作る（Builder → Critic のループ）

- `src/world/layout.ts` のデモを、survey.md の要素に置き換える。足りない部品は `src/world/kit.ts` に
  builder として追加する（+Z 向きで作り、`place()` で置く／部材は `strut()` で 2 点間に張る）。
- 乗り物が一覧に無ければ `src/player/vehicle.ts` に種類を 1 行＋builder で追加する。
- 季節の生き物・天気は `src/world/particles.ts`。

**Builder と Critic を交互に回す（並列にしない）。1 周 = 1 イテレーション。回数は作り込みレベルに従う。**

**最初は粗く、後で細かく。** 最初の 1〜2 イテレーションは配置・スケール・道の形・光だけを合わせ、
細部には手を付けない（配置が動けば細部は作り直しになる）。配置が Critic で合格してから、
作り込みレベルに応じて `DETAIL.md` の手順でディテールアップに入る。

- 撮影：`npm run dev` を起動した状態で `npm run shoot -- --out=iter-N`
  （全 viewpoint × 朝・昼・夕・夜。初回は `npx playwright install chromium`）。
  ブラウザペインから `await __shot('name', 1600, 900, { vp: '03', time: 17.5 })` でも撮れる。
- **Critic** は `shots/iter-N/vp-NN@*.jpg` を `refs/place/NN.jpg` と並べて見て、`docs/critic-iter-N.md` に：
  1. 場所らしさ：トップ 10 の要素がそろっているか、配置・スケール・道の曲がりが合っているか
  2. テイスト：`refs/style/`（無ければ brief.md の言葉）と比べて色・光・線・空気感
  3. 時刻 4 種の破綻（夜が暗すぎる、夕方が濁る、点くべきものが点かない）
  4. brief.md の禁止事項に触れていないか
  5. **修正リスト上位 5 件**（具体的に：どのファイルの何を、どう変えるか）
  Builder は次のイテレーションでそれを直す。
- 毎回 `npm run explore`（自動運転で全コースを走り切る／エラーゼロ）と `npm run typecheck` を通す。

## ディテールアップ（完成後も使える）

`DETAIL.md` にディテールアップの手順とチェックリストがある。Phase 2 の後半で使うほか、
完成後にユーザーが「家をディテールアップして」「03 の地点を作り込んで」「自転車を作り込んで」と
対象を指定してきたら、DETAIL.md に従って作業する。

## Phase 3：VR と仕上げ

- VR は `src/xr/xr.ts` が土台（着座・一人称・水平固定・スナップターン・周辺減光・手首の時計）。
  VR では墨線パスが切れるので、VR で見て線が必要な物には `addOutline()` を付ける。
- 実機がある場合は `npm run dev:vr`（HTTPS、LAN 公開）で、ヘッドセットのブラウザから開いてもらう。
  酔い・操作感は実機でしか分からないので、ユーザーに確認を頼む。
- 性能：`__scene.bench()` / shoot の report.json。デスクトップ 1080p で平均 90fps 以上、VR は 72fps 相当を目安。
  重いときは draw call を疑う（`__scene.stats().calls`）。静的な物は `ctx.add()` に入れてベイクさせる。

## 完了条件（Done means）

- `npm run dev` で起動し、キーボード＋マウスで移動できる。VR ボタンから VR でも移動できる。
- Critic が 10 地点 × 4 時刻の全カットで「場所らしさ」「テイスト」とも合格。
- 作り込みレベルに応じたディテールアップが終わっている（標準：トップ 10、じっくり：全カテゴリ）。
- `npm run explore` 合格、`npm run typecheck` 合格、コンソールエラーゼロ。
- `docs/brief.md`（約束と禁止事項）、`docs/survey.md`、`docs/critic-iter-*.md` がある。
- `README.md` をこの世界用に書き換え、`CLAUDE.md` に今回見つけた罠を追記、`TASKS.md` に残課題。

## 複雑すぎたら削る順番

1. 季節の生き物 2. 遠景の細部 3. 時刻の早送り 4. 乗り物の細部 5. 水面の表現
**絶対に削らない**：セル調＋線、空と雲、時刻インジケーターと時刻による変化、選んだ乗り物（または徒歩）、
場所らしさトップ 10、VR で移動できること、VR の酔い対策。

## 進め方

- 各イテレーションの最後に「変えたこと」「見つけたこと」「確認できなかったこと（どこを見たか）」を書く。
- 見た目で信じず数値で確かめる（到達・衝突・FPS・エラー数）。この手の世界のバグは、例外も出ずスクショでも普通に見えることが多い。
